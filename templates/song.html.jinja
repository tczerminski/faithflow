<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Kościół Zmartwychwstałego | Tarnowskie Góry | Śpiewnik</title>
    <link rel="stylesheet" href="/static/main.css">
    <link rel="manifest" href="/static/manifest.json">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description"
          content="Kościół Zmartwychwstałego | Tarnowskie Góry | {{ song.title }}">
    <link rel="icon" type="image/jpeg" href="/static/icons/icon-48x48.png">
</head>
<body>
<article class="song">
    <a href="/">Powrót do listy pieśni</a>
    <header class="song__header">
        <h1 class="song__title">{{ song.title }}</h1>
    </header>
    <section class="song__lyrics" aria-labelledby="song__title">
        {% for verse in song.verses %}
            <p class="song__verse">
                {% for line in verse.lines %}
                    {{ line }}<br>
                {% endfor %}
            </p>
        {% endfor %}
    </section>
</article>
</body>
</html>
